<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>

        <main>
            <% if (products.length > 0) { %>
                <ul>
                    <% products.forEach(({productData, quantity}) => { %>
                        <li> <%= productData.title %>  (<%= quantity %>)</li>
                    <% }) %>
                </ul>
            <% } else { %>
                <h1>No products</h1>
            <% } %>

        </main>

<%- include('../includes/end.ejs') %>